"""Tests for the agent loop — Phase 1."""

from __future__ import annotations


class TestAgentLoop:
    """Unit tests for AgentLoop orchestration.

    TODO (Phase 1): mock LLMClient to return predetermined tool calls,
    assert agent loop iterates correctly, respects max_iterations,
    and produces expected final text.
    """

    def test_placeholder(self) -> None:
        assert True
